<?php
	// Fichier de configuration de la base de donnée
	// A remplir et à mettre dans le dossier parent.

	$host = "";
	$user = "";
	$pwd = "";
	$base = "";

	// Config en local
	if($_SERVER['HTTP_HOST'] == "localhost")
	{
		$host = "";
		$user = "";
		$pwd = "";
		$base = "";
	}
	// Config en ligne
	else
	{
		$host = "";
		$user = "";
		$pwd = "";
		$base = "";
	}